<%- include('../partials/head', { title: title }) %>
<%- include('../partials/header') %>
<main class="page">
  <p><a href="/messages" class="back-link">&larr; <%= t('common.back') %></a></p>
  <h1><%= t('messages.conversation_with') %> <%= otherUser ? otherUser.name : '' %></h1>
  <p class="muted"><%= t('messages.about_listing') %>: <a href="/containers/<%= conversation.container_id %>"><%= conversation.origin_city %> &rarr; <%= conversation.destination_city %> (<%= conversation.container_number %>)</a></p>

  <div class="safety-notice">
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 4 5v6c0 5 3.4 8.6 8 11 4.6-2.4 8-6 8-11V5l-8-3Z"/><path d="M12 8v5M12 16h.01"/></svg>
    <p><%= t('messages.safety_notice') %></p>
  </div>

  <div class="thread">
    <% messages.forEach(function(m) { %>
      <div class="message-bubble <%= m.sender_id === currentUser.id ? 'mine' : 'theirs' %>">
        <p><%= m.body %></p>
        <time><%= new Date(m.created_at).toLocaleString(lang) %></time>
      </div>
    <% }) %>
  </div>

  <form method="post" action="/messages/<%= conversation.id %>/reply" class="form reply-form">
    <textarea name="body" rows="2" placeholder="<%= t('messages.reply_placeholder') %>" required></textarea>
    <button type="submit" class="btn btn-primary"><%= t('messages.send') %></button>
  </form>

  <% if (isShipper) { %>
    <div class="card rating-card">
      <h2><%= myRating ? t('messages.rating_update_title') : t('messages.rating_title') %></h2>
      <form method="post" action="/messages/<%= conversation.id %>/rate" class="rating-form">
        <div class="star-rating">
          <% for (var s = 5; s >= 1; s--) { %>
            <input type="radio" id="score<%= s %>" name="score" value="<%= s %>" <%= myRating && myRating.score === s ? 'checked' : '' %> required>
            <label for="score<%= s %>" title="<%= s %>">&#9733;</label>
          <% } %>
        </div>
        <textarea name="comment" rows="2" placeholder="<%= t('messages.rating_comment_placeholder') %>"><%= myRating ? myRating.comment || '' : '' %></textarea>
        <button type="submit" class="btn btn-ghost btn-sm"><%= t('common.save') %></button>
      </form>
    </div>
  <% } %>
</main>
<%- include('../partials/bottom-nav') %>
<%- include('../partials/scripts') %>
